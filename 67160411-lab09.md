# 📌Lab09-Todo

# ปฏิบัติการที่ 9: สร้าง App ง่ายๆ ด้วย Async/Await

## 👉ส่วนที่ 1: การทำความเข้าใจโค้ด

### คำถาม 1.1

**เหตุใดจึงต้องแยก duration และ timeLeft ออกจากกัน อธิบายความแตกต่าง**

- `duration` = เวลาตั้งต้นของ timer
- `timeLeft` = เวลาที่เหลืออยู่ขณะ timer กำลังนับถอยหลัง

แยกไว้เพื่อให้สามารถรีเซ็ตกลับไปเวลาเดิมได้ และใช้แสดงผลเวลาปัจจุบัน

---

### คำถาม 1.2

**formatTime() ฟังก์ชันนี้ทำความสำคัญอะไร? ถ้าไม่มีฟังก์ชันนี้ app จะเป็นยังไง**

จัดรูปแบบเวลาให้แสดงผลอ่านง่าย ใช้แปลงเวลาจากวินาทีให้เป็นรูปแบบ `MM:SS`

ถ้าไม่มีฟังก์ชันนี้

- เวลาอาจแสดงไม่สวย เช่น `5:3:9 แทน 05:03:09`
- ผู้ใช้จะอ่านเวลาได้ยาก

---

### คำถาม 1.3

**อธิบายข้อแตกต่างระหว่าง setInterval() และ setTimeout()**

setTimeout()

- ทำงาน ครั้งเดียว หลังจากครบเวลาที่กำหนด
- เหมาะกับงานที่ต้องการหน่วงเวลาแล้วค่อยทำ เช่น แจ้งเตือนครั้งเดียว

setInterval()

- ทำงาน ซ้ำ ๆ ทุกช่วงเวลาที่กำหนด
- เหมาะกับงานที่ต้องอัปเดตต่อเนื่อง เช่น นาฬิกา หรือรีเฟรชข้อมูล

---

## 👉ส่วนที่ 2: ฟังก์ชันการทำงาน

### คำถาม 2.1

**เมื่อผู้ใช้คลิกปุ่ม “▶️ เริ่ม” ครั้งแรก เกิดอะไรขึ้นในลำดับใด (ระบุขั้นตอนอย่างน้อย 4 ขั้น)**

1. ตรวจสอบว่า timer กำลังทำงานหรือไม่
2. เปลี่ยน `isRunning` เป็น true
3. เรียก `setInterval()` เพื่อนับเวลา
4. ลดค่า `timeLeft` ทุก 1 วินาที
5. อัปเดต UI

---

### คำถาม 2.2

**ในฟังก์ชัน toggleTimer() ทำไมจึงต้องมี clearInterval() ก่อนแล้วจึงตั้ง task.isRunning = false**

เพื่อหยุด timer เดิมก่อน ป้องกัน timer ทำงานซ้อนกัน

---

### คำถาม 2.3

**ถ้า timer หมดเวลา จะเกิดความผิดพลาดใดหากไม่มี clearInterval()**

- Timer จะยังทำงานต่อ
- เวลาอาจติดลบ
- App ทำงานผิดพลาด

---

## 👉ส่วนที่ 3: DOM และ UI

### คำถาม 3.1

**ฟังก์ชัน renderTasks() สามารถแยกออกเป็นฟังก์ชันย่อยได้กี่ฟังก์ชัน จงเสนอการแยกที่เหมาะสม**

- `createTaskElement()`
- `updateTimerDisplay()`
- `attachEventListeners()`

---

### คำถาม 3.2

**ใช้วิธีใดในการอัปเดต UI ทุกครั้งที่ timer เปลี่ยนแปลง มีวิธีการอื่นนอกเหนือจำนวนปัจจุบันหรือไม่**

วิธีปัจจุบัน:

- เรียก `renderTasks()` ใหม่

วิธีอื่น:

- อัปเดตเฉพาะ element ที่เปลี่ยน
- ใช้ Framework เช่น React

---

### คำถาม 3.3

**“keypress” event listener ทำความสำคัญอะไร หากลบออกไป app จะเป็นยังไง**

ทำให้กด Enter เพื่อเพิ่ม task ได้ ถ้าลบออก ผู้ใช้ต้องคลิกปุ่มเพิ่มเท่านั้น

---

## 👉ส่วนที่ 4: Web Audio API

### คำถาม 4.1

**อธิบายหน้าที่ของการตั้งค่า:**

- oscillator.frequency.value = 800
  -> กำหนดความถี่เสียง
- oscillator.type = "sine"
  -> กำหนดรูปแบบเสียงให้ฟังนุ่ม
- gainNode.gain.setValueAtTime(0.3, audioContext.currentTime)
  -> กำหนดความดังของเสียง

---

### คำถาม 4.2

**ทำไมเสียง alarm จึงใช้เวลา 0.5 วินาที แล้วหยุด ถ้าเพิ่มเป็น 2 วินาทีจะส่งผลต่อ UX อย่างไร**
ใช้เวลา 0.5 วินาที

- สั้น กระชับ ไม่รบกวน

ถ้าเพิ่มเป็น 2 วินาที

- อาจทำให้รำคาญผู้ใช้

---

## 👉ส่วนที่ 5: Array Methods

### คำถาม 5.1

**ในฟังก์ชัน deleteTask(id) ใช้ filter() เพื่ออะไร เขียนวิธีอื่นแทน filter() ได้หรือไม่**

ลบ task ที่มี id ตรงกับที่ส่งมา โดยสร้าง array ใหม่ที่ ไม่มี task นั้น

วิธีอื่น:

- ใช้ `splice()` หลังหา index
- ใช้ findIndex() แล้ว splice()
- ใช้ for loop แล้วลบเอง

---

### คำถาม 5.2

**ในฟังก์ชัน renderTasks() ใช้ map() เพื่อทำอะไร ถ้าใช้ forEach() แทนจะดีหรือไม่**

ใช้สร้าง HTML จาก task

ใช้ `forEach()` ได้  
แต่ `map()` เหมาะกับการสร้าง array ใหม่มากกว่า

---

## 👉ส่วนที่ 6: ข้อบกพร่องและการแก้ไข

### คำถาม 6.1

**หากผู้ใช้เพิ่ม task 2 อัน แล้วคลิก “เริ่ม” บน task ทั้ง 2 อันพร้อมๆ กัน จะเกิดอะไรขึ้น**

Timer ทั้งสองจะทำงานพร้อมกัน อาจทำให้ performance ลดลง

---

### คำถาม 6.2

**ถ้า timer กำลังเดินอยู่ และผู้ใช้รีเฟรชหน้า (F5) task จะหายไป ทำไม วิธีแก้ปัญหาคือ**

เพราะข้อมูลเก็บใน memory เท่านั้น
วิธีแก้ ใช้ `LocalStorage`

---

### คำถาม 6.3

**ในการทดสอบ app ที่ timer 30 นาที จะใช้เวลานานไปหรือไม่ เสนอวิธีทดสอบที่เร็วขึ้น**

วิธีแก้:

- ลดเวลาเหลือ 10–30 วินาที
- ใช้ mock timer

---

## 👉ส่วนที่ 7: Enhancement

### คำถาม 7.1

**หากต้องการเพิ่มฟังก์ชัน “รีเซ็ต timer” คืนไปเป็น 30 นาที โค้ดจะเป็นยังไง**

```js
function resetTimer(task) {
  task.timeLeft = task.duration;
  task.isRunning = false;
  clearInterval(task.intervalId);
  renderTasks();
}
```

### คำถาม 7.2

**วิธีใดในการเก็บ tasks ไว้ใน LocalStorage เพื่อไม่ให้หายเมื่อรีเฟรชหน้า**

วิธีเก็บ tasks ไว้ใน LocalStorage คือ:

- บันทึกข้อมูลลง LocalStorage (แปลงเป็น JSON ก่อน)
  localStorage.setItem("tasks", JSON.stringify(tasks));

- ดึงข้อมูลกลับมาใช้ตอนโหลดหน้าเว็บ
  const savedTasks = JSON.parse(localStorage.getItem("tasks")) || [];

JSON.stringify() ตอนบันทึก
ใช้ JSON.parse() ตอนดึงข้อมูล
เรียกตอนโหลดหน้า เพื่อให้ข้อมูลไม่หายหลังรีเฟรช

---

### คำถาม 7.3

**ถ้าต้องการให้ผู้ใช้กำหนดเวลา timer เอง จะต้องแก้ไขฟังก์ชันไหนบ้าง**

ให้ผู้ใช้กำหนดเวลาเอง

ต้องแก้:

- ฟอร์ม input เวลา
- ฟังก์ชันเพิ่ม task
- การตั้งค่า duration

---

## 👉ส่วนที่ 8: Concepts เชิงลึก

### คำถาม 8.1

**ทำไม renderTasks() จึงต้องเรียกหลายครั้ง มีวิธี DRY ที่ดีกว่าหรือไม่**

เพื่อให้ UI อัปเดตตามข้อมูลล่าสุด และมีวิธี DRY แยก update UI เฉพาะส่วน

---

### คำถาม 8.2

**ถ้าเปลี่ยนจาก setInterval เป็น requestAnimationFrame จะส่งผลดีหรือไม่**

ไม่ดีกว่า สำหรับงานแบบ timer

- setInterval() เหมาะกับงานที่ทำทุก ๆ 1 วินาที
- requestAnimationFrame() ทำงานตาม frame (~60 ครั้ง/วินาที) เหมาะกับ animation มากกว่า

ถ้าใช้ requestAnimationFrame() กับ timer
→ จะทำงานถี่เกินจำเป็น
→ เปลืองทรัพยากรมากกว่า

งานนับถอยหลังแบบนี้ ใช้ setInterval() เหมาะกว่า

---

### คำถาม 8.3

**ในโค้ดที่ใช้ setInterval() มี Memory Leak ที่อาจเกิดขึ้นได้หรือไม่**

มีโอกาสเกิดได้
ถ้าลบ task ตอน setInterval() ยังทำงานอยู่ และไม่ได้ clearInterval()
→ interval จะยังรันต่อ ทำให้เกิด Memory Leak ได้

---

# 📌Lab09-Pomodoro

# ปฏิบัติการที่ 9: Pomodoro Timer App

## 👉ส่วนที่ 1: ความเข้าใจเทคนิค Pomodoro

### คำถาม 1.1

**เทคนิค Pomodoro คืออะไร เหตุใดจึงเลือกใช้เวลา 25 นาที สำหรับการทำงาน**

เทคนิคแบ่งเวลาทำงาน 25 นาที พัก 5 นาที ทำครบ 4 รอบพักยาว 15 นาที
ช่วยเพิ่มโฟกัส ลดความล้า และป้องกัน burnout

---

### คำถาม 1.2

**หากต้องการปรับเวลา Work/Break ให้แตกต่างจาก default (25/5) จะต้องแก้ไขโค้ดส่วนไหน**

แก้ค่าใน `<input>` และใน JS:

```js
state.workDuration = parseInt(workTimeInput.value) * 60;
state.breakDuration = parseInt(breakTimeInput.value) * 60;
```

### คำถาม 1.3

**ทำไมหลังจาก 4 รอบ (pomodoros) จึงมี Long Break ขนาด 15 นาที ประโยชน์คืออะไร**

เพื่อให้สมองพักฟื้นหลังโฟกัสหลายรอบ เพิ่มประสิทธิภาพระยะยาว

---

## 👉ส่วนที่ 2: การจัดโครงสร้าง State

### คำถาม 2.1

**ปัจจุบัน app เก็บ state อะไรบ้าง เช่น: เวลาที่เหลือ Phase (work/break) Rounds counter …อื่นๆ**

- isRunning
- isWorkPhase
- timeLeft
- totalTime

---

### คำถาม 2.2

**ทำไมจึงต้องมี rounds state หากแค่เก็บ phase ก็พอ**

เพื่อรู้ว่าครบ 4 รอบหรือยังและใช้ตัดสินใจ Long Break

---

### คำถาม 2.3

**ถ้าต้องการให้ Pomodoro Count อัปเดต เมื่อจบแต่ละ Work phase จะแก้ไขฟังก์ชัน completePhase() ยังไง**

เพิ่มตอนจบ Work phase:

- state.completedSessions++;

---

## 👉ส่วนที่ 3: Timer Logic

### คำถาม 3.1

**อธิบายการทำงานของ setInterval() ในส่วน timer - จะเกิดอะไรขึ้นทุก 1 วินาที**

- ลดเวลา
- เช็คหมดเวลา
- อัปเดต UI

---

### คำถาม 3.2

**เหตุใดจึงต้องมี timerInterval เป็น global variable ถ้าไม่มี app จะเป็นยังไง**

เพื่อใช้ clearInterval() ป้องกัน timer ซ้อน

---

### คำถาม 3.3

**ถ้าผู้ใช้คลิก “เริ่ม” 2 ครั้งติดต่อกัน จะเกิดอะไรขึ้น มีวิธีแก้หรือไม่**

จะเกิดหลาย interval → เวลาเดินเร็ว
ต้องเช็ค if (state.isRunning) return;

---

## 👉ส่วนที่ 4: DOM & UI

### คำถาม 4.1

**ฟังก์ชัน updateDisplay() ทำอะไร ต้องเรียกจากที่ไหนบ้าง**

อัปเดตเวลา, phase, progress bar, ปุ่ม และ stats

---

### คำถาม 4.2

**formatTime() ใช้ padding ด้วย padStart(2, "0") เพื่ออะไร ถ้าไม่มี timer จะแสดง 5:3 แทน 05:03 ได้หรือไม่**

ทำให้เวลาเป็น 2 หลัก เช่น 05:03 แทน 5:3

---

### คำถาม 4.3

**Progress bar คำนวณเปอร์เซ็นต์ยังไง เขียนสูตรออกมา**

((totalTime - timeLeft) / totalTime) \* 100

---

## 👉ส่วนที่ 5: Events & Controls

### คำถาม 5.1

**ปุ่มควบคุมมีอะไรบ้าง (Start, Pause, Reset, Settings) แต่ละปุ่มเรียกฟังก์ชันไหน**

- Start/Pause → startTimer()
- Reset → resetTimer()
- Settings → change event

---

### คำถาม 5.2

**ทำไม Pause ต้องหยุด timer ด้วย clearInterval() หากไม่ทำจะไปไป**

ถ้าไม่ทำ timer จะยังเดินอยู่

---

### คำถาม 5.3

**Reset button ควรตั้งค่ากลับเป็นอะไร: timeLeft phase rounds timerInterval**

- timeLeft = workDuration
- isWorkPhase = true
- currentSession = 1
- clearInterval()
- isRunning = false

---

## 👉ส่วนที่ 6: Notifications & Sound

### คำถาม 6.1

**ฟังก์ชัน playSound() ใช้เทคนิคอะไรในการสร้างเสียง (Web Audio API? HTML Audio?)**

Web Audio API (AudioContext, Oscillator)

---

### คำถาม 6.2

**ทำไม notification ควร work/break ต่างกันอย่างไร เสียง alarm ที่ดีควรเป็นยังไง**

Work → เสียงเตือนจบงาน
Break → เสียงเริ่มพัก
ควรสั้น ไม่ตกใจเกินไป

---

### คำถาม 6.3

**ถ้าต้องการเพิ่ม Desktop Notification ด้วย Browser Notification API จะแก้ไขฟังก์ชัน completePhase() ยังไง**

```js
Notification.requestPermission();
new Notification("Work Complete!");
```

## 👉ส่วนที่ 7: Progress Tracking & Stats

### คำถาม 7.1

**Stats ต้องติดตามข้อมูลอะไร: Pomodoros ที่เสร็จแล้ว, เวลารวมที่ทำงาน, Long breaks ที่เกิดขึ้น**

- จำนวน Pomodoro ที่เสร็จ
- เวลารวมที่โฟกัส
- จำนวน Long Break

---

### คำถาม 7.2

**ตำแหน่งที่ดีที่สุดในการอัปเดต stats คือที่ไหน**

ตอนจบ Work phase

---

### คำถาม 7.3

**วิธีการบันทึก stats ไว้ใน LocalStorage ได้อย่างไร**

```js
localStorage.setItem("stats", JSON.stringify(state));
```

## 👉ส่วนที่ 8: Bug Detection

### คำถาม 8.1

**ถ้า timer กำลังเดินอยู่ แล้วผู้ใช้เปิด DevTools ขนาดใหญ่ timer จะยังคงเดินต่อหรือไม่**

ยังคงเดินต่อ
การเปิด DevTools ไม่ได้หยุดการทำงานของ `setInterval()`  
JavaScript จะยังทำงานตามปกติ

---

### คำถาม 8.2

**ถ้า completePhase() เรียก ในขณะที่ timer ยังวิ่งอยู่ จะเกิด memory leak ได้หรือไม่**

อาจเกิด memory leak หรือ interval ซ้อนกันได้  
ถ้าไม่ clearInterval() ก่อนสร้าง timer ใหม่

---

### คำถาม 8.3

**ใน mobile ถ้าผู้ใช้ล็อก screen ขณะ timer เดิน timer จะหยุดหรือยังคงเดิน วิธีแก้คืออะไร**

- JavaScript อาจถูก pause เมื่อหน้าจอถูกล็อก  
  timer จะไม่เดินตามเวลาจริง
- วิธีแก้:
  ใช้ timestamp (Date.now()) คำนวณเวลาจริงแทนการพึ่ง setInterval อย่างเดียว

---

## 👉ส่วนที่ 9: Architecture & Code Quality

### คำถาม 9.1

**ถ้าต้องการแยก Timer logic ออกจาก UI ควรสร้าง class อย่างไร**

สร้าง class สำหรับจัดการเวลาโดยไม่ผูกกับ DOM เช่น:

```js
class PomodoroTimer {
  constructor(work, breakTime) {
    this.workDuration = work;
    this.breakDuration = breakTime;
    this.isRunning = false;
  }

  start() {}
  pause() {}
  reset() {}
}
```

### คำถาม 9.2

**Functions ที่มีมากควรจัดไว้ใน object เพื่อจัดการได้ดีขึ้นหรือไม่**

ควรจัดไว้ใน object หรือ class
เพราะจะช่วย:

- ลด global scope
- โค้ดอ่านง่ายขึ้น
- ทดสอบ (test) ได้ง่ายขึ้น

---

### คำถาม 9.3

**Code reusability - ส่วนไหนของโค้ดสามารถทำให้เป็น utility functions ได้**

- formatTime()
- calculateProgress()
- saveToLocalStorage()
- loadFromLocalStorage()

สามารถแยกไว้ในไฟล์ utils.js ได้

---

## 👉ส่วนที่ 10: Enhancements

### คำถาม 10.1

**วิธีเพิ่ม Dark Mode ควรแก้ไข CSS/JS ส่วนไหนบ้าง**

1. เพิ่ม class .dark ใน CSS
2. ใช้ JavaScript toggle class ที่ body:
   document.body.classList.toggle("dark");
3. กำหนดสี background และ text ใหม่ใน CSS

---

### คำถาม 10.2

**วิธีบันทึกประวัติการใช้งาน (session history) คืออะไร**

- เก็บข้อมูลแต่ละ session ในรูปแบบ array
- บันทึกลง localStorage
- โหลดกลับมาแสดงในหน้าเว็บ

---

### คำถาม 10.3

**วิธีเพิ่ม Keyboard Shortcuts (Space = Start/Pause, R = Reset) ควรใช้ event ไหน**

```js
document.addEventListener("keydown", (e) => {
  if (e.code === "Space") startTimer();
  if (e.key === "r") resetTimer();
});
```

---

# 📌Lab09-Weather App

# ปฏิบัติการที่ 9: Weather App (Fetch API)

## 👉ส่วนที่ 1: Fetch API & HTTP Requests

### คำถาม 1.1

**Fetch API คืออะไร ความแตกต่างระหว่าง Fetch API และ XMLHttpRequest คืออะไร**

- Fetch API คือวิธีส่ง HTTP request ไปหา API แล้วรับ response กลับมา
- ใช้กับ Promise และ async/await ได้ง่าย
- เขียนโค้ดสั้น อ่านง่ายกว่า XMLHttpRequest
- XMLHttpRequest โค้ดยาวกว่า ซับซ้อนกว่า และใช้ callback เยอะ

---

### คำถาม 1.2

**โค้ดนี้ทำอะไร อธิบายการทำงานแต่ละขั้นตอน**

```js
fetch(url)
  .then((response) => response.json())
  .then((data) => console.log(data))
  .catch((error) => console.error(error));
```

1. fetch(url) → ส่ง request ไปที่ API
2. then(response => response.json()) → แปลง response เป็น JSON
3. .then(data => console.log(data)) → เอาข้อมูลมาใช้ (แสดงใน console)
4. .catch() → ถ้ามี error ให้แสดง error

---

### คำถาม 1.3

**ถ้า API response ล้มเหลว (network error) จะถูก handle ที่ไหน .catch() จะจับได้หรือไม่**

จะถูกจับที่ .catch()
ถ้าใช้ async/await → จะถูกจับใน catch ของ try-catch

---

## 👉ส่วนที่ 2: Geocoding API

### คำถาม 2.1

**Geocoding API ทำหน้าที่อะไร ทำไมจึงต้องใช้ก่อนเรียก Weather API**

[text](https://geocoding-api.open-meteo.com/v1/search?name=Bangkok)

แปลง ชื่อเมือง → latitude/longitude
ต้องใช้ก่อน Weather API เพราะ Weather API ต้องใช้พิกัด

---

### คำถาม 2.2

**When user searches “Bangkok” ควรส่ง request ไปที่ endpoint ไหนก่อน ได้ข้อมูลอะไรจาก response**

1. ส่ง request ไป [text](https://geocoding-api.open-meteo.com/v1/search?name=Bangkok)
2. ได้ข้อมูล: latitude, longitude, country
   แล้วเอา lat/lon ไปเรียก Weather API ต่อ

---

### คำถาม 2.3

**ถ้า user ค้นหาเมือง “Nonthaburi” ซึ่งชื่อเดียวกับเมืองอื่น app จะตัดสินใจอย่างไร**

ในโค้ดใช้ count=1

- เลือกผลลัพธ์ตัวแรก
- App แสดงเมืองแรกที่ API ส่งกลับมา

---

## 👉ส่วนที่ 3: Weather Data & Parameters

### คำถาม 3.1

**Weather API ต้องการ parameters อะไรบ้าง (latitude, longitude, …อื่นๆ)**

- latitude
- longitude
- current (temperature, humidity, wind ฯลฯ)
- hourly (temperature รายชั่วโมง)
- timezone

---

### คำถาม 3.2

```js
const params =
  "?latitude=13.7&longitude=100.5&current=temperature_2m,humidity,weather_code";
```

**Parameters นี้ request ข้อมูลอะไร ทำไมต้องใช้ weather_codee**

- ขอ:อุณหภูมิ, ความชื้น, weather_code
  ใช้ weather_code เพื่อเอาไปแปลงเป็น emoji/คำอธิบาย เช่น ☀️ 🌧️

---

### คำถาม 3.3

**ความแตกต่างระหว่าง current weather และ hourly forecast คืออะไร**

- current = สภาพอากาศตอนนี้
- hourly = พยากรณ์รายชั่วโมงล่วงหน้า

---

## 👉ส่วนที่ 4: Async/Await Pattern

### คำถาม 4.1

**ทำไมจึงใช้ async/await แทน .then().catch() มีข้อดีอะไรบ้าง**

- โค้ดอ่านง่ายเหมือน synchronous
- ไม่ซ้อน .then() หลายชั้น
- จัดการ error ด้วย try-catch ได้ง่าย

---

### คำถาม 4.2

```js
async function getWeather(city) {
  try {
    const response = await fetch(url);
    const data = await response.json();
    return data;
  } catch (error) {
    console.error(error);
  }
}
```

**ทำอะไร ถ้าไม่ใช้ code จะเป็นยังไง**

รอให้ Promise ทำงานเสร็จก่อน
ถ้าไม่ใช้ → โค้ดจะทำงานต่อทันที อาจได้ค่า undefined

---

### คำถาม 4.3

**try-catch block ใช้เพื่ออะไร ตกลง error ที่ catch ได้อะไร**

ใช้ดัก error
จับ network error หรือ API error

---

## 👉ส่วนที่ 5: DOM Manipulation

### คำถาม 5.1

**เมื่อได้ข้อมูล weather มา app ต้องแสดงผล element ใดบ้าง**

- cityName
- temperature
- humidity
- windSpeed
- pressure
- visibility
- hourly forecast

---

### คำถาม 5.2

**Loading spinner ปรากฏในไหน เมื่อไหร่ควรแสดง/ซ่อน**

อยู่ใน <div id="loading">
แสดงตอนเริ่ม fetch
ซ่อนเมื่อได้ข้อมูลแล้ว

---

### คำถาม 5.3

**ถ้าต้องอัปเดต temperature display ทุก 10 นาที ควรใช้ setInterval() หรือ setTimeout()**

ควรใช้ setInterval()

---

## 👉ส่วนที่ 6: Data Storage (LocalStorage)

### คำถาม 6.1

**LocalStorage ใช้เพื่ออะไร บันทึก recent cities ยังไง**

เก็บ recent cities

---

### คำถาม 6.2

```js
localStorage.setItem("recentCities", JSON.stringify(cities));
```

**ทำไมต้องใช้ JSON.stringify() ถ้าเก็บ object โดยตรงจะเป็นอย่างไร**

เพราะ localStorage เก็บได้แค่ string
ถ้าเก็บ object ตรง ๆ → จะกลายเป็น [object Object]

---

### คำถาม 6.3

**เมื่อ refresh หน้า app ควร restore recent cities จากไหน โค้ดเป็นยังไง**

```js
let state = {
  recentCities: JSON.parse(localStorage.getItem("recentCities")) || [],
};
```

## 👉ส่วนที่ 7: Error Handling & Edge Cases

### คำถาม 7.1

**ถ้า user ค้นหาเมืองที่ไม่มี (เช่น “XYZ”) app จะแสดง error อย่างไร**

Geocoding API ไม่มี results
throw error
showError("ไม่พบเมืองนี้")

---

### คำถาม 7.2

**ถ้า network ไม่มี (offline) fetch จะ throw error หรือ return success**

fetch จะ throw error
→ ถูกจับใน catch

---

### คำถาม 7.3

**เมื่อ hover หรือ click ที่ recent city button โปรแกรมจะทำอะไร**

จะเรียก:

```js
fetchWeather(city.latitude, city.longitude, city);
```

## 👉ส่วนที่ 8: Weather Display

### คำถาม 8.1

**Weather code 0 = Clear sky, 1 = Partly cloudy, 80 = Drizzle… เลือก icon/emoji ยังไง**

ใช้ weather_code map กับ object:

```js
const weatherCodes = {
  0: "☀️ ท้องฟ้าแจ่มใส",
};
```

### คำถาม 8.2

**ความแตกต่างระหว่าง อุณหภูมิ (°C), ความชื้น (%), ความเร็วลม (m/s) คืออะไร**

°C = อุณหภูมิ
% = ความชื้น
m/s = ความเร็วลม

---

### คำถาม 8.3

**Hourly forecast แสดง 24 ชั่วโมง ต้องเลือก element ไหนจาก response**

data.hourly.temperature_2m
data.hourly.time

---

## 👉ส่วนที่ 9: Responsive Design & Mobile

### คำถาม 9.1

**ใน mobile screen weather card ควร มี layout อย่างไร (vertical/horizontal)**

ควรเป็น vertical (เรียงลง)

---

### คำถาม 9.2

**เมื่อผู้ใช้ rotate device grid/flex ควรตัดสินใจอย่างไร**

ใช้ media query ปรับ grid เป็น 1 column

---

### คำถาม 9.3

**Input field และ button บน mobile ต้องมี font size เท่าไร (ควรมากพอให้แตะง่าย)**

อย่างน้อย 16px ขึ้นไป เพื่อแตะง่าย

---

## 👉ส่วนที่ 10: Code Architecture & Enhancement

### คำถาม 10.1

**ถ้าต้องสร้าง Weather class เพื่อจัดการ logic อย่างไร**

```js
class WeatherApp {
  async searchCity(cityName) {}
  async fetchWeather(lat, lon) {}
  displayWeather(data) {}
}
```

รวม logic ไว้ใน class เพื่อจัดการง่ายขึ้น

---

### คำถาม 10.2

**วิธีเพิ่ม Favorite Cities feature ยังไง ต้องแก้ไข HTML/CSS/JS ส่วนไหนบ้าง**

ต้องแก้:
HTML → เพิ่มปุ่ม ⭐
JS → เก็บ favorites ใน localStorage
CSS → style ปุ่ม

---

### คำถาม 10.3

**วิธีเพิ่ม Dark Mode ด้วย CSS variables ได้อย่างไร**

ใช้ CSS variables:

```js
:root {
  --bg-color: white;
}

.dark {
  --bg-color: #222;
}
```

แล้ว toggle class dark ใน JS

---
